// Re-export the shared AI Advisor page component so that when company owner navigates to /own-ai-advisor,
// Next.js uses the correct (owner) layout.
export { default } from "../../(export-manager)/em-ai-advisor/page";

